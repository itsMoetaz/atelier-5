<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
        }

        table {
            border-collapse: collapse;
            width: 80%;
            margin: 20px auto;
            background-color: #fff;
            border: 2px solid #3498db;
            border-radius: 8px;
        }

        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ccc;
        }

        th {
            background-color: #3498db;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        tr:nth-child(odd) {
            background-color: #e5e5e5;
        }

        .actions {
            text-align: center;
        }

        .actions a {
            display: inline-block;
            padding: 8px 15px;
            text-decoration: none;
            background-color: #e74c3c;
            color: white;
            border: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .actions a:hover {
            background-color: #c0392b;
        }
    </style>
</head>
<body>
<form method="post" action='/minmax'>
    <label>Min</label>
    <input type="number" name="min">
    <label>Max</label>
    <input type="number" name="max">
    <input type="submit" value="Rechercher">
</form>

<table>
    <thead>
        <tr>
                    <th>id</th>

            <th>Username</th>
            <th>Mail</th>
            <th>nb_books</th>

            <th>Actions</th>

        </tr>
    </thead>
    <tbody>
<a href="{{ path('add') }}">Ajouter</a>

        {% for repo in response %}
            <tr>
                <td>{{ repo.id }}</td>
                <td>{{ repo.username }}</td>
                <td>{{ repo.email }}</td>
                <td>{{ repo.nbBooks }}</td>

                <td>
                    <a href="{{ path('modifier_Author', {'id': repo.id}) }}" class="btn btn-primary" style="color: green">Modifier</a>
                 <a href="{{ path('deleteAuthor', {'id': repo.id}) }}" class="btn btn-primary" style="color: green">Supprimer</a>

                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>

</body>
</html>
